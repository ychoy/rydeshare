<div class="chat twelve wide column">
  <div class="ui basic segment">
    <h3 class="ui header">
      Rydeshare Details
    </h3>
    <div class="ui divider"></div>
    <div class="content">
      <p>Driver: <%= User.where(id:@trip.user_id).first.first_name %> <%= User.where(id:@trip.user_id).first.last_name %> </p>
      <p>Address: <%= @trip.address %></p>
      <p>Coordinates: <%= @trip.latitude %>, <%= @trip.longitude %></p>
    </div>
  </div>
</div>
